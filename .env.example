# Public-facing demo message (still read only by the server)
APP_MESSAGE="Hello from the deployment environment"

# Placeholder only. The application renders a masked preview.
APP_SECRET_HINT="replace-with-a-demo-secret"

# Set by the deployment process to confirm which deployment is running.
BUILD_TIME="2026-01-01T00:00:00Z"
